# Development / test dependencies (not needed to run the pipeline).
-r requirements.txt
pytest>=7.4
pyyaml>=6.0                       # parse CITATION.cff in the metadata-integrity tests
tomli>=2.0; python_version < "3.11"   # tomllib backport for Python 3.10 (metadata tests)
